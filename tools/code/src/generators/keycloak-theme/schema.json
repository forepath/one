{
  "$schema": "https://json-schema.org/schema",
  "$id": "KeycloakTheme",
  "title": "Keycloakify Keycloak Theme Generator",
  "description": "Nx generator for creating a new Angular-based Keycloak theme using Keycloakify",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "domain": {
      "type": "string",
      "enum": ["agenstra", "forepath", "shared"],
      "default": "shared",
      "description": "Product domain folder and Nx project name prefix",
      "x-prompt": "Which domain should this app belong to?"
    },
    "prefix": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "default": "app",
      "x-prompt": "What prefix would you like to use?"
    }
  },
  "required": ["name"]
}
