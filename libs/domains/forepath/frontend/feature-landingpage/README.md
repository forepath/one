# forepath-frontend-feature-landingpage

This library was generated with [Nx](https://nx.dev).

## Project estimate debug panel

The `/pricing/estimate` page hides the temporary estimator state debug panel by default. Press **Ctrl+E** to toggle it.

## Running unit tests

Run `nx test forepath-frontend-feature-landingpage` to execute the unit tests.
