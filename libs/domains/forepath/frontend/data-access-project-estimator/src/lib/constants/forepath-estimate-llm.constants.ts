/** Greedy decoding for stable estimates across repeated prompts. */
export const FOREPATH_ESTIMATE_LLM_TEMPERATURE = 0;

/** Disable nucleus sampling so temperature 0 stays deterministic. */
export const FOREPATH_ESTIMATE_LLM_TOP_P = 1;

export const FOREPATH_ESTIMATE_LLM_MAX_TOKENS = 1536;
