/**
 * Agents must emit this exact marker in assistant text before verification runs.
 * Keep in sync with automation preamble in `tickets-prototype-prompt.utils.ts`.
 */
export const AGENSTRA_AUTOMATION_COMPLETE = 'AGENSTRA_AUTOMATION_COMPLETE';
